# ── SERVER CONFIGURATION ──────────────────────
PORT=5000
FLASK_PORT=18080
NODE_ENV=development

# ── DATABASE CONFIGURATION ────────────────────
# Use 127.0.0.1 instead of localhost to avoid IPv6 connection errors
MONGODB_URI=mongodb://127.0.0.1:27017/tassia_school

# ── SECURITY (CRITICAL: MUST BE IDENTICAL) ────
# This secret is used by both Node.js (userRoutes.js) and Flask (app.py)
JWT_SECRET=7d8f9e2a1b3c4d5e6f7g8h9i0j1k2l3m4n5o6p7q8r9s0t1u2v3w4x5y6z7a8b9c
JWT_EXPIRES_IN=8h

# ── FRONTEND INTEGRATION ──────────────────────
FRONTEND_URL=http://localhost:3000